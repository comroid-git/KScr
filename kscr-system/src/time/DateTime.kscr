package org.comroid.kscr.time;

import org.comroid.kscr.time.Date;
import org.comroid.kscr.time.Time;

public final class DateTime : Date, Time {
    public DateTime(int days, int seconds) : Date(days), Time(seconds) {
        stdio <<- "DateTime is " + days + ":" + seconds + endl;
    }
}
