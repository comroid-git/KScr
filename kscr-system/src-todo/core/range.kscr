package org.comroid.kscr.core;

public native class range<numtype> : Sequencable<numtype> where numtype : num = int {
    native numtype Start { get; }
    native numtype End { get; }
    native numtype Step { get; }
    native bool test(numtype x);
    native numtype accumulate(numtype x);
}
